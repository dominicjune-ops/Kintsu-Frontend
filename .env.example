# Environment Configuration for Kintsu Frontend

# API Backend URL
# Production: Leave empty (uses relative /api path - same origin, no CORS)
# Development: http://localhost:3001 (if running server locally)
VITE_API_URL=

# Feature Flags
VITE_ENABLE_CHAT=true
VITE_ENABLE_KINTO_AVATAR=true

# Environment
VITE_ENV=production

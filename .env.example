# Environment Configuration for Kintsu Frontend

# API Backend URL (choose one)
# Option 1: Use internal RAG backend (localhost for dev, deployed URL for prod)
VITE_API_URL=http://localhost:3001

# Option 2: Use external CareerCoach.ai backend
# VITE_API_URL=https://api.careercoach.ai

# Feature Flags
VITE_ENABLE_CHAT=true
VITE_ENABLE_KINTO_AVATAR=true

# Environment
VITE_ENV=production

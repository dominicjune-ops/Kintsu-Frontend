import { TrustPillars } from '../TrustPillars';
import { ThemeProvider } from '../ThemeProvider';

export default function TrustPillarsExample() {
  return (
    <ThemeProvider>
      <TrustPillars />
    </ThemeProvider>
  );
}
